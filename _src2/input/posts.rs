use std::path::{Path, PathBuf};

use anyhow::*;

use crate::input::Post;

pub fn find(src: &Path) -> Result<Vec<String>> {
    todo!()
}

pub fn load(src: &Path, id: &str) -> Result<Post> {
    todo!()
}